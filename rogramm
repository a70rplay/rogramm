-- украл = мама шалава
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local TopBar = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local Credit = Instance.new("TextLabel")
local DupeButton = Instance.new("TextButton")
local UICorner3 = Instance.new("UICorner")
local OpenCaseButton = Instance.new("TextButton")
local UICorner4 = Instance.new("UICorner")

ScreenGui.Parent = Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.DisplayOrder = 999999999

MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(247,247,247)
MainFrame.Position = UDim2.new(0.05, 0, 0.3, 0)
MainFrame.Size = UDim2.new(0, 350, 0, 210)
MainFrame.ZIndex = 999999999

UICorner.CornerRadius = UDim.new(0, 16)
UICorner.Parent = MainFrame

TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(255,226,226)
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.ZIndex = 999999999

Title.Parent = TopBar
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "0как"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18
Title.ZIndex = 999999999

Credit.Parent = MainFrame
Credit.BackgroundTransparency = 1
Credit.Position = UDim2.new(0, 15, 0, 50)
Credit.Size = UDim2.new(1, -30, 0, 25)
Credit.Font = Enum.Font.Gotham
Credit.Text = "https://t.me/infarogramm | От @hwpvo с любовью"
Credit.TextColor3 = Color3.fromRGB(150, 150, 160)
Credit.TextSize = 14
Credit.TextXAlignment = Enum.TextXAlignment.Left
Credit.ZIndex = 999999999

OpenCaseButton.Parent = MainFrame
OpenCaseButton.BackgroundColor3 = Color3.fromRGB(255,199,199)
OpenCaseButton.Position = UDim2.new(0, 20, 0, 110)
OpenCaseButton.Size = UDim2.new(1, -40, 0, 50)
OpenCaseButton.Font = Enum.Font.GothamBold
OpenCaseButton.Text = "Автофарм"
OpenCaseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenCaseButton.TextSize = 18
OpenCaseButton.ZIndex = 999999999

UICorner4.CornerRadius = UDim.new(0, 12)
UICorner4.Parent = OpenCaseButton

local dragging = false
local dragStart, startPos

TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

TopBar.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

TopBar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

local opening = false
OpenCaseButton.MouseButton1Click:Connect(function()
    opening = not opening
    if opening then
        OpenCaseButton.BackgroundColor3 = Color3.fromRGB(255,153,153)
        OpenCaseButton.Text = "Стоп"
        task.spawn(function()
            while opening do
                pcall(function()
                    ReplicatedStorage:WaitForChild("ByteNetReliable"):FireServer(
                        buffer.fromstring("\021\001")
                    )
                end)
                task.wait(0.2)
                local keepAll = Players.LocalPlayer.PlayerGui:FindFirstChild("CasesFrame")
                if keepAll then
                    keepAll = keepAll:FindFirstChild("Frame")
                    if keepAll then keepAll = keepAll:FindFirstChild("Contain") end
                    if keepAll then keepAll = keepAll:FindFirstChild("Case") end
                    if keepAll then keepAll = keepAll:FindFirstChild("List") end
                    if keepAll then keepAll = keepAll:FindFirstChild("KeepAll") end
                    if keepAll then
                        for _, conn in getconnections(keepAll.MouseButton1Click) do
                            pcall(conn.Fire, conn)
                        end
                    end
                end
                task.wait(0.2)
            end
        end)
    else
        OpenCaseButton.BackgroundColor3 = Color3.fromRGB(255,207,223)
        OpenCaseButton.Text = "Автофарм"
    end
end)

wait(0.5)local ba=Instance.new("ScreenGui")
local ca=Instance.new("TextLabel")local da=Instance.new("Frame")
local _b=Instance.new("TextLabel")local ab=Instance.new("TextLabel")ba.Parent=game.CoreGui
ba.ZIndexBehavior=Enum.ZIndexBehavior.Sibling;ca.Parent=ba;ca.Active=true
ca.BackgroundColor3=Color3.new(0.176471,0.176471,0.176471)ca.Draggable=true
ca.Position=UDim2.new(0.698610067,0,0.098096624,0)ca.Size=UDim2.new(0,370,0,52)
ca.Font=Enum.Font.SourceSansSemibold;ca.Text="АНТИАФК"ca.TextColor3=Color3.new(0,1,1)
ca.TextSize=22;da.Parent=ca
da.BackgroundColor3=Color3.new(0.196078,0.196078,0.196078)da.Position=UDim2.new(0,0,1.0192306,0)
da.Size=UDim2.new(0,370,0,107)_b.Parent=da
_b.BackgroundColor3=Color3.new(0.176471,0.176471,0.176471)_b.Position=UDim2.new(0,0,0.800455689,0)
_b.Size=UDim2.new(0,370,0,21)_b.Font=Enum.Font.Arial;_b.Text="@infarogramm"
_b.TextColor3=Color3.new(0,1,1)_b.TextSize=20;ab.Parent=da
ab.BackgroundColor3=Color3.new(0.176471,0.176471,0.176471)ab.Position=UDim2.new(0,0,0.158377,0)
ab.Size=UDim2.new(0,370,0,44)ab.Font=Enum.Font.ArialBold;ab.Text="Статус - Активно"
ab.TextColor3=Color3.new(0,1,1)ab.TextSize=20;local bb=game:service'VirtualUser'
game:service'Players'.LocalPlayer.Idled:connect(function()
bb:CaptureController()bb:ClickButton2(Vector2.new())
ab.Text="Роблокс попытался кикнуть его - мы его остановили :D"wait(2)ab.Text="Статус - Активно"end)
-- украл = мама шалава
